openapi: 3.1.0
info:
  title: Stock Lens 第一阶段 API
  version: 0.1.0
  description: Stock Lens 第一阶段行情数据同步与查询 API。
servers:
  - url: http://localhost:30078
tags:
  - name: health
    description: 健康检查与就绪检查。
  - name: market
    description: 股票、日 K、交易日历和枚举查询。
  - name: sync
    description: 行情数据同步任务与同步日志。
paths:
  /healthz:
    get:
      tags: [health]
      summary: 存活检查
      responses:
        "200":
          description: 进程存活。
  /readyz:
    get:
      tags: [health]
      summary: 就绪检查
      responses:
        "200":
          description: 服务已就绪。
        "503":
          description: 服务未就绪。
  /api/v1/market/stocks:
    get:
      tags: [market]
      summary: 查询股票列表
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - name: keyword
          in: query
          schema:
            type: string
        - name: exchange
          in: query
          schema:
            $ref: "#/components/schemas/Exchange"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/StockStatus"
        - $ref: "#/components/parameters/Market"
        - $ref: "#/components/parameters/AssetType"
      responses:
        "200":
          description: 股票列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/StockList"
        "400":
          $ref: "#/components/responses/Error"
  /api/v1/market/stocks/{symbol}:
    get:
      tags: [market]
      summary: 查询股票详情
      parameters:
        - $ref: "#/components/parameters/Symbol"
        - $ref: "#/components/parameters/Market"
        - $ref: "#/components/parameters/AssetType"
      responses:
        "200":
          description: 股票详情。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/StockDetail"
        "404":
          $ref: "#/components/responses/Error"
  /api/v1/market/stocks/{symbol}/daily-k-lines:
    get:
      tags: [market]
      summary: 查询日 K 列表
      parameters:
        - $ref: "#/components/parameters/Symbol"
        - $ref: "#/components/parameters/StartDate"
        - $ref: "#/components/parameters/EndDate"
        - $ref: "#/components/parameters/Market"
        - $ref: "#/components/parameters/AssetType"
      responses:
        "200":
          description: 日 K 数据列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/DailyKLine"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
  /api/v1/market/stocks/{symbol}/latest-daily-k-line:
    get:
      tags: [market]
      summary: 查询最新日 K
      parameters:
        - $ref: "#/components/parameters/Symbol"
        - $ref: "#/components/parameters/Market"
        - $ref: "#/components/parameters/AssetType"
      responses:
        "200":
          description: 最新日 K；没有数据时返回 null。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        oneOf:
                          - $ref: "#/components/schemas/DailyKLine"
                          - type: "null"
        "404":
          $ref: "#/components/responses/Error"
  /api/v1/market/trade-calendars:
    get:
      tags: [market]
      summary: 查询交易日历
      parameters:
        - $ref: "#/components/parameters/StartDate"
        - $ref: "#/components/parameters/EndDate"
        - $ref: "#/components/parameters/Market"
        - name: exchange
          in: query
          schema:
            $ref: "#/components/schemas/Exchange"
      responses:
        "200":
          description: 交易日历记录。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/TradeCalendar"
        "400":
          $ref: "#/components/responses/Error"
  /api/v1/market/trade-calendars/latest-open-day:
    get:
      tags: [market]
      summary: 查询最近开市日
      parameters:
        - $ref: "#/components/parameters/Market"
        - name: exchange
          in: query
          schema:
            $ref: "#/components/schemas/Exchange"
      responses:
        "200":
          description: 最近开市日。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/LatestOpenDay"
        "409":
          $ref: "#/components/responses/Error"
  /api/v1/market/trade-calendars/is-open:
    get:
      tags: [market]
      summary: 判断指定日期是否开市
      parameters:
        - name: date
          in: query
          schema:
            type: string
            format: date
        - $ref: "#/components/parameters/Market"
        - name: exchange
          in: query
          schema:
            $ref: "#/components/schemas/Exchange"
      responses:
        "200":
          description: 开市状态。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IsOpenDay"
        "404":
          $ref: "#/components/responses/Error"
  /api/v1/market/meta/enums:
    get:
      tags: [market]
      summary: 查询枚举元数据
      responses:
        "200":
          description: 枚举元数据。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EnumMetadata"
  /api/v1/market/sync/stocks:
    post:
      tags: [sync]
      summary: 创建股票主数据同步任务
      description: "需要在请求头中提供 `Authorization: Bearer <admin-token>`。"
      security:
        - adminToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SyncStocksRequest"
      responses:
        "200":
          $ref: "#/components/responses/SyncTaskCreated"
        "401":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"
  /api/v1/market/sync/trade-calendars:
    post:
      tags: [sync]
      summary: 创建交易日历同步任务
      description: "需要在请求头中提供 `Authorization: Bearer <admin-token>`。"
      security:
        - adminToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SyncTradeCalendarsRequest"
      responses:
        "200":
          $ref: "#/components/responses/SyncTaskCreated"
        "400":
          $ref: "#/components/responses/Error"
        "401":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"
  /api/v1/market/sync/daily-k-lines:
    post:
      tags: [sync]
      summary: 创建日 K 同步任务
      description: "需要在请求头中提供 `Authorization: Bearer <admin-token>`。"
      security:
        - adminToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SyncDailyKLinesRequest"
      responses:
        "200":
          $ref: "#/components/responses/SyncTaskCreated"
        "400":
          $ref: "#/components/responses/Error"
        "401":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"
  /api/v1/market/sync/tasks:
    get:
      tags: [sync]
      summary: 查询同步任务列表
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/SyncTaskStatus"
      responses:
        "200":
          description: 同步任务列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SyncTaskList"
  /api/v1/market/sync/tasks/{task_uid}:
    get:
      tags: [sync]
      summary: 查询同步任务详情
      parameters:
        - $ref: "#/components/parameters/TaskUID"
      responses:
        "200":
          description: 同步任务详情。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SyncTask"
        "404":
          $ref: "#/components/responses/Error"
  /api/v1/market/sync/tasks/{task_uid}/logs:
    get:
      tags: [sync]
      summary: 查询同步任务日志
      parameters:
        - $ref: "#/components/parameters/TaskUID"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/SyncLogStatus"
        - name: symbol
          in: query
          schema:
            type: string
      responses:
        "200":
          description: 同步日志列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SyncLogList"
        "404":
          $ref: "#/components/responses/Error"
components:
  securitySchemes:
    adminToken:
      type: http
      scheme: bearer
      description: "Admin Token 认证。受保护的同步创建接口必须传入 `Authorization: Bearer <admin-token>`。"
  parameters:
    Page:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1
    PageSize:
      name: page_size
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    Market:
      name: market
      in: query
      schema:
        $ref: "#/components/schemas/Market"
    AssetType:
      name: asset_type
      in: query
      schema:
        $ref: "#/components/schemas/AssetType"
    Symbol:
      name: symbol
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9]{6}$"
    TaskUID:
      name: task_uid
      in: path
      required: true
      schema:
        type: string
        minLength: 26
        maxLength: 26
    StartDate:
      name: start_date
      in: query
      schema:
        type: string
        format: date
    EndDate:
      name: end_date
      in: query
      schema:
        type: string
        format: date
  responses:
    Error:
      description: 错误响应。
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Envelope"
    SyncTaskCreated:
      description: 同步任务已创建。
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Envelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/SyncTaskCreation"
  schemas:
    Envelope:
      type: object
      required: [code, message, request_id, data]
      properties:
        code:
          type: string
        message:
          type: string
        request_id:
          type: string
        data: {}
    Pagination:
      type: object
      required: [page, page_size, total, total_pages]
      properties:
        page:
          type: integer
        page_size:
          type: integer
        total:
          type: integer
          format: int64
        total_pages:
          type: integer
    Market:
      type: string
      enum: [CN]
      default: CN
    AssetType:
      type: string
      enum: [STOCK]
      default: STOCK
    Exchange:
      type: string
      enum: [SSE, SZSE, BSE]
      default: SSE
    Board:
      type: string
      enum: [MAIN, GEM, STAR, BSE, UNKNOWN]
    StockStatus:
      type: string
      enum: [LISTED, DELISTED, PAUSED]
    DataSource:
      type: string
      enum: [TUSHARE, MOCK]
    SyncTaskType:
      type: string
      enum: [SYNC_STOCK_MASTER, SYNC_TRADE_CALENDARS, SYNC_DAILY_K_LINES]
    SyncTaskStatus:
      type: string
      enum: [PENDING, RUNNING, SUCCESS, FAILED, PARTIAL_SUCCESS]
    SyncLogStatus:
      type: string
      enum: [SUCCESS, FAILED, WARNING]
    Stock:
      type: object
      properties:
        symbol:
          type: string
        name:
          type: string
        market:
          $ref: "#/components/schemas/Market"
        asset_type:
          $ref: "#/components/schemas/AssetType"
        exchange:
          $ref: "#/components/schemas/Exchange"
        board:
          $ref: "#/components/schemas/Board"
        area:
          type: string
        industry:
          type: string
        status:
          $ref: "#/components/schemas/StockStatus"
        list_date:
          type: [string, "null"]
          format: date
        delist_date:
          type: [string, "null"]
          format: date
        data_source:
          $ref: "#/components/schemas/DataSource"
        synced_at:
          type: string
          format: date-time
    StockDetail:
      allOf:
        - $ref: "#/components/schemas/Stock"
        - type: object
          properties:
            latest_daily_k_line:
              oneOf:
                - $ref: "#/components/schemas/DailyKLine"
                - type: "null"
    StockList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Stock"
        pagination:
          $ref: "#/components/schemas/Pagination"
    DailyKLine:
      type: object
      properties:
        symbol:
          type: string
        market:
          $ref: "#/components/schemas/Market"
        asset_type:
          $ref: "#/components/schemas/AssetType"
        trade_date:
          type: string
          format: date
        open:
          type: string
        high:
          type: string
        low:
          type: string
        close:
          type: string
        pre_close:
          type: string
        change:
          type: string
        pct_change:
          type: string
        pct_change_unit:
          type: string
          enum: [percent]
        volume:
          type: string
        volume_unit:
          type: string
          enum: [lot]
        amount:
          type: string
        amount_unit:
          type: string
          enum: [thousand_cny]
        data_source:
          $ref: "#/components/schemas/DataSource"
        synced_at:
          type: string
          format: date-time
    TradeCalendar:
      type: object
      properties:
        market:
          $ref: "#/components/schemas/Market"
        exchange:
          $ref: "#/components/schemas/Exchange"
        cal_date:
          type: string
          format: date
        is_open:
          type: boolean
        pretrade_date:
          type: [string, "null"]
          format: date
        data_source:
          $ref: "#/components/schemas/DataSource"
        synced_at:
          type: string
          format: date-time
    LatestOpenDay:
      type: object
      properties:
        market:
          $ref: "#/components/schemas/Market"
        exchange:
          $ref: "#/components/schemas/Exchange"
        date:
          type: string
          format: date
    IsOpenDay:
      allOf:
        - $ref: "#/components/schemas/LatestOpenDay"
        - type: object
          properties:
            is_open:
              type: boolean
            pretrade_date:
              type: [string, "null"]
              format: date
    EnumMetadata:
      type: object
      properties:
        markets:
          type: array
          items:
            type: string
        asset_types:
          type: array
          items:
            type: string
        exchanges:
          type: array
          items:
            type: string
        boards:
          type: array
          items:
            type: string
        stock_statuses:
          type: array
          items:
            type: string
        data_sources:
          type: array
          items:
            type: string
        sync_task_types:
          type: array
          items:
            type: string
        sync_task_statuses:
          type: array
          items:
            type: string
    SyncTaskCreation:
      type: object
      properties:
        task_uid:
          type: string
        status:
          $ref: "#/components/schemas/SyncTaskStatus"
    SyncTask:
      type: object
      properties:
        task_uid:
          type: string
        task_type:
          $ref: "#/components/schemas/SyncTaskType"
        status:
          $ref: "#/components/schemas/SyncTaskStatus"
        market:
          $ref: "#/components/schemas/Market"
        asset_type:
          $ref: "#/components/schemas/AssetType"
        data_source:
          $ref: "#/components/schemas/DataSource"
        total_items:
          type: integer
          format: int64
        processed_items:
          type: integer
          format: int64
        success_items:
          type: integer
          format: int64
        failed_items:
          type: integer
          format: int64
        request_id:
          type: string
        started_at:
          type: [string, "null"]
          format: date-time
        finished_at:
          type: [string, "null"]
          format: date-time
        error_msg:
          type: string
        created_at:
          type: string
          format: date-time
    SyncTaskList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SyncTask"
        pagination:
          $ref: "#/components/schemas/Pagination"
    SyncLog:
      type: object
      properties:
        task_uid:
          type: string
        step:
          type: string
        status:
          $ref: "#/components/schemas/SyncLogStatus"
        market:
          $ref: "#/components/schemas/Market"
        asset_type:
          $ref: "#/components/schemas/AssetType"
        symbol:
          type: string
        data_source:
          $ref: "#/components/schemas/DataSource"
        message:
          type: string
        error_detail:
          type: string
        affected_rows:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
    SyncLogList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SyncLog"
        pagination:
          $ref: "#/components/schemas/Pagination"
    SyncStocksRequest:
      type: object
      properties:
        market:
          $ref: "#/components/schemas/Market"
        asset_type:
          $ref: "#/components/schemas/AssetType"
    SyncTradeCalendarsRequest:
      type: object
      required: [start_date, end_date]
      properties:
        market:
          $ref: "#/components/schemas/Market"
        exchange:
          $ref: "#/components/schemas/Exchange"
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
    SyncDailyKLinesRequest:
      type: object
      properties:
        market:
          $ref: "#/components/schemas/Market"
        asset_type:
          $ref: "#/components/schemas/AssetType"
        symbol:
          type: string
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
