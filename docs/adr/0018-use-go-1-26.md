# Use Go 1.26

Phase 1 uses Go 1.26 as the project language version, with `go.mod`, local development, and CI aligned to that version. This gives the backend a current stable Go baseline while keeping the runtime, tooling, and dependency compatibility target explicit for all contributors.
